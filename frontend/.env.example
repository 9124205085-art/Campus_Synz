# Local development
VITE_API_URL=http://localhost:5000/api

# Production (Vercel) — set in Vercel → Settings → Environment Variables, then redeploy
# VITE_API_URL=https://YOUR-RENDER-SERVICE.onrender.com/api
